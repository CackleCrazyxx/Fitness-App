login = Window(app,title="Log In", height = 600, bg = "antiquewhite1", layout = 'grid') #this creates the login window
login.hide()
logo = Picture(login, image="Edited Logo.PNG", width = 200, height = 200, grid = [1,0])
login_message = Text(login, text=" Welcome Back!", grid = [1,1]) #this tells the user what the window is for
login_user1 = Text(login, text= "Username:", grid = [0,2]) #this tells the user what to enter
user_textbox = TextBox(login, text="",width = 30, grid = [1,2]) #this gives the user space to enter the username
login_pwd = Text(login, text= "Password:", grid = [0,3]) #this tells the user what to enter for the password box
pwd_textbox = TextBox(login, text="",width = 30,  grid = [1,3], hide_text = True)#this gives the user space to enter the password
#
login_update_pwd =  PushButton(login, text="Change Password", command=update_pwd_window, width = 15, grid = [0,4]) #this creates the sign_up button
log_in = PushButton(login, text="Log In", command=login_user,width = 15, grid = [1,5]) # this allows the user to try and login
btn_back = PushButton(login, text="Back", command=back, width = 15, grid = [1,6]) #this allows the user to go back to the main menu 
